#include <bits/stdc++.h>
using namespace std;

void changeNumber(vector<int> arr_)
{
    for (int i = 0; i < arr_.size(); i++)
    {
        if (arr_[i] == arr_[i + 1])
            arr
    }
}

int main()
{
    int t;
    cin >> t;
    while (t)
    {
        int n;
        vector<int> arr;
        cin >> t;
        for (int i = 0; i < n; i++)
        {
            cin >> arr[i];
        }

        t--;
    }
}