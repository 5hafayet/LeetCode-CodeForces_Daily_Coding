#include <bits/stdc++.h>
using namespace std;

int sumOfStr(char arr[])
{
    int sum = 0;
    for (int i = 0; i < arr.size(); i++)
    {
        /* code */
    }
}

int main()
{
    int t;
    cin >> t;
    while (t)
    {
        int n, k;
        string s;
        vector<char> arr;
        cin >> n >> k;
        cin >> s;
        strcpy(arr, s.c_str());

        t--;
    }
}